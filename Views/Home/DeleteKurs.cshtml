@model Kurs

<p>
   <b>@Model.Baslik </b> isimli kursu silme işlemini onaylıyor musunuz? 
   <form asp-action="DeleteKurs" method="post">
       <input type="hidden" asp-for="@Model.Id">
       <button type="submit" class="btn btn-sm btn-danger">Evet</button>
       <a asp-action="GetKurslar" class="btn btn-sm btn-warning">Hayır</a>
   </form>
</p>