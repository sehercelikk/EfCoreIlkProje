@model Egitmen

<p>
   <b>@Model.AdSoyad</b> isimli eğitmeni silme işlemini onaylıyor musunuz? 
   <form asp-action="DeleteEgitmen" method="post">
       <input type="hidden" asp-for="@Model.Id">
       <button type="submit" class="btn btn-sm btn-danger">Evet</button>
       <a asp-action="GetEgitmenler" class="btn btn-sm btn-warning">Hayır</a>
   </form>
</p>